import ScrollToTop from "react-scroll-to-top";
const BackToTop = () => {
    return (
        <div>
           <ScrollToTop smooth style={{top:'60%'}} />
        </div>
    );
}

export default BackToTop;